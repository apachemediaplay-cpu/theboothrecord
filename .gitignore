# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local secrets — copy .env.example to .env and fill in (never commit .env)
.env

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.firebase

# Local Claude Code tooling (preview launch config, etc.) — not part of the app
.claude
# Firebase functions
functions/node_modules
functions/.env

# Generated: copy of src/data/venues.json (single source), made by prep.mjs + deploy predeploy
functions/venues.json
# Generated: copy of dist/index.html (built app shell), made by the deploy predeploy hook
functions/index.html
.venv/

# Reel production output
captures/
outputs/
versions/
